import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.engineering.toolbox',
  appName: 'Engineering Toolbox',
  webDir: 'dist'
};

export default config;
